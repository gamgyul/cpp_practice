#include <vector>
#include <iostream>
#include <string>
#include <list>
#include <deque>
int
main(int argc, char **argv) {
    
    int input;
    std::vector<int> vi;

    auto it = vi[0];
    
    std::cout << it <<std::endl;
    
    
    return 0;
}